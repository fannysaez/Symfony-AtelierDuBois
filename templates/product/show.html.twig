{% extends 'base.html.twig' %}

{% block title %}L'Atelier du Bois - Nos produits{% endblock %}

{% block main %}

<section class="flex flex-column items-center pt-50 gap-50">
  <div class="product-page flex flex-row gap-15">
    <img src="{{ asset(product.cover) }}" alt="{{ product.name }}">

    <div class="product-info flex flex-column justify-between gap-15">
      <div class="flex flex-column gap-15">
        <div>
          <h1>{{ product.name }}</h1>
          <h4>{{ product.priceTTC|format_currency('EUR') }}</h4>
        </div>
        <p>{{ product.description }}</p>
      </div>
      <a class="btn btn-fit btn-primary" href="">Commander</a>
    </div>
  </div>
</section>

<section class="atelier-presentation flex flex-column">
  <h2>
    Passionnés par la beauté, nous créons dans notre Atelier de Bois des
    créations nouvelles, modernes et individuelles qui, avec leur longue durée
    de vie, constituent la base de nos produits. Avec notre fabrication et nos
    produits, nous essayons de laisser une empreinte la plus petite possible.
  </h2>

  <div class="projects-section flex justify-between">
    <h3>Projets</h3>
    <div class="projects-list flex flex-column">
      <a class="project-card"> Installateur </a>
      <a class="project-card"> Bilan : Journée portes ouvertes 2024 </a>
      <a class="project-card"> Prochain après-midi de projet le 28 juin </a>
    </div>
  </div>

  <div class="products-section flex flex-column gap-25">
    <h3>Nos produits</h3>

    <div class="products-grid">

      {% for product in latestProducts %}
      {% include "_partials/card.twig" %}
      {% endfor %}

      <a class="product-link" href="{{ path('product_index') }}">
        <div>
          <h4>Voir Plus</h4>
        </div>
      </a>

    </div>
  </div>
</section>

{% endblock %}